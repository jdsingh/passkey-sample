rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the Firebase Admin SDK (server-side only).
    // Block all direct client SDK access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
